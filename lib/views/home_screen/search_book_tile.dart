part of 'home_screen.dart';

class SearchBookTile extends StatelessWidget {
  const SearchBookTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
